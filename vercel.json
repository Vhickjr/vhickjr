{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "command": "npm install --legacy-peer-deps && npm run build"
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
